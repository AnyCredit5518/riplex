"""Metadata lookup and planning.

Sub-modules:
    provider  — abstract metadata provider interface and DTOs
    planner   — TMDb metadata planning (builds PlannedMovie/PlannedShow)
    sources/  — concrete provider implementations (TMDb)
"""
